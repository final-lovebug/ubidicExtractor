# 후보 검토 판단 기록

> `result.json`의 후보를 사람이 검토하며 내린 **의미론적 판단**("이건 맞다/틀리다/이렇게 정의해야 한다")을 기록한다.
> `verification-log.md`(검증 시 남긴 원문 텍스트)나 `TEST-CHECKLIST.md`의 확인 이력(통과 여부)과는 다르다 — 이건 **채점기가 못 잡는, 제품 의미 자체에 대한 판단**이다. SPEC.md §1이 말하는 "확정은 사람이 한다"가 실제로 일어나는 자리다.
> 이 판단은 이후 프롬프트(`01_role.md`/`02_forbidden.md`) 수정이나 `gold.csv` 개정 논의의 근거가 된다.

---

## 2026-09-09 · "주문"(HOMOGRAPH) 정의 통일과 "결제 취소"/"주문 취소"(SYNONYM 후보 `c-6`)의 관계

**검토 대상**: `out/result.json`의 `c-6`(SYNONYM, forms: `["결제 취소", "주문 취소"]`, confidence 0.9)와 아직 못 찾은 HOMOGRAPH `G6`("주문").

**판단**: 지금 문서 맥락만 보면 "결제 취소"(결제 승인 **전** 철회)와 "주문 취소"(결제API명세v2.md — 주문 생성 이후 이행 전 취소, 즉 결제 승인 **후** 이행 전)는 서로 다른 시점을 가리켜서 별개가 맞다.

**단, 조건부 결론**: "주문"의 동형이의 두 갈래(기획팀 = 장바구니에 담긴 시점 / 개발팀 = 결제 승인 후 확정)를 **기획팀 정의로 통일**하기로 하면 얘기가 달라진다 — "주문 취소"가 가리키는 "주문"이 기획팀 정의(장바구니 시점)로 재해석되면, "주문 취소"는 "결제 취소"와 **같은 시점을 가리키는 동의어**가 되어 하나로 묶여야 한다.

**왜 이게 중요한가**: `c-6`이 맞는 판정인지 여부가 "주문"의 정의를 어느 쪽으로 통일하느냐에 **종속적**이다. 즉 `G6`(주문 HOMOGRAPH)을 먼저 정확히 잡아내고 팀이 어느 쪽 정의로 갈지 정하는 게, `c-6` 같은 SYNONYM 판정의 정확도보다 선행돼야 하는 문제다.

**후속 조치**: `G6`("주문")을 잡아내는 프롬프트 작업을 먼저 진행하기로 함 — 아래 대화에서 방법 제시.

---
